SELECT * 
FROM Employees
where salary>50000;

SELECT emp_name,city,salary
FROM Employees
WHERE city ='Delhi';

SELECT *
FROM employees
WHERE age>=28 AND salary >45000;

SELECT emp_name
,city,salary
FROM employees
WHERE department_id = 1 OR department_id=4;

SELECT emp_name,city,salary
FROM employees
WHERE department_id IN (1,4);

SELECT *
FROM employees
WHERE salary BETWEEN 40000 and 60000;

SELECT emp_name,city,salary
FROM employees
WHERE department_id IN (1,4);

SELECT *
FROM employees
WHERE salary BETWEEN 40000 and 60000;

SELECT emp_name,city,department_id
FROM employees
WHERE department_id != 2;

SELECT department_id,COUNT(emp_name)
FROM employees
group by department_id;

SELECT city , AVG(salary)
FROM employees
GROUP BY city;

SELECT department_id , AVG(salary)
FROM employees
GROUP BY department_id;

SELECT department_id , MAX(salary) , MIN(salary)
FROM employees
GROUP BY department_id;

SELECT department_id , SUM(salary)
FROM employees
GROUP BY department_id;

SELECT city,count(emp_name)
FROM employees
GROUP BY city
HAVING  count(emp_name)>=2;

SELECT emp_name,salary
FROM employees
WHERE salary = (
  SELECT MAX(salary)
  FROM employees);

SELECT *
FROM orders
WHERE amount = ( SELECT MAX(amount)
          FROM orders);

SELECT product_name,price
FROM products
WHERE price = (SELECT MIN(price)
FROM products);

SELECT customer_name , city , age
FROM customers
WHERE age = (SELECT MAX(age)
FROM customers);

SELECT *
FROM orders
WHERE amount =(SELECT AVG(amount)
  FROM orders);
  
SELECT emp_name,salary,city
FROM employees
WHERE salary<(SELECT AVG(salary)
FROM employees);

SELECT project_name as project ,budget as prices
FROM projects
WHERE budget>(SELECT AVG(budget)
FROM projects);

